server:
  port: 9003

spring:
  application:
    name: sentinel-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848  #nacos地址


management:
  endpoints:
    web:
      exposure:
        include: '*'